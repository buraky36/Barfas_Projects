#include "pin_manager.h"
#include "shift_register_map.h"

// Dummy source file to satisfy CMake component registration.
// Can be used later for hardware initialization routines that encompass the
// whole board.
void device_config_init(void) {
  // Platform level hardware initializations if needed
}
